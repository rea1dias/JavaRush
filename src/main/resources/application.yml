spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/javarushdb
    username: postgres
    password: Shakpakbaba.2003
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
